<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/output.css" rel="stylesheet">
    <title>Canvas Items Missing Fields Report</title>
</head>
<body>
    <div class="w-full bg-gray-900 min-h-screen text-white text-lg p-8">
        <h1 class="w-full text-center p-2 text-2xl font-bold mb-6">Canvas Items Missing Fields Report</h1>
        
        <div class="mb-4">
            <p class="text-gray-400">Total Issues Found: <%= data.length %></p>
        </div>

        <div class="overflow-x-auto">
            <table class="w-full border-collapse">
                <thead>
                    <tr>
                        <th class="border border-gray-600 bg-gray-800 p-3 text-left">#</th>
                      
                        <th class="border border-gray-600 bg-gray-800 p-3 text-left font-bold">Item Type</th>
                        <th class="border border-gray-600 bg-gray-800 p-3 text-left font-bold">Missing Fields</th>

                        <th class="border border-gray-600 bg-gray-800 p-3 text-left text-sm font-normal text-gray-400">Presentation ID</th>
                        <th class="border border-gray-600 bg-gray-800 p-3 text-left text-sm font-normal text-gray-400">Slide ID</th>
                        
                      
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach((item, index) => { %>
                        <tr class="hover:bg-gray-700">
                            <td class="border border-gray-600 bg-gray-800 p-3"><%= index + 1 %></td>
                           
                            
                            <td class="border border-gray-600 bg-gray-800 p-3">
                                <span class="px-3 py-1.5 bg-blue-600 rounded-full text-base font-semibold">
                                    <%= item.itemName %>
                                </span>
                            </td>
                            <td class="border border-gray-600 bg-gray-800 p-3">
                                <div class="flex flex-wrap gap-2">
                                    <% item.missingFields.forEach(field => { %>
                                        <span class="px-2.5 py-1.5 bg-red-600 rounded-lg text-base font-medium">
                                            <%= field %>
                                        </span>
                                    <% }); %>
                                </div>
                            </td>



                            <td class="border border-gray-600 bg-gray-800 p-3">
                                <span class="font-mono text-xs text-gray-400">
                                    <%= item.presentationId %>
                                </span>
                            </td>
                            <td class="border border-gray-600 bg-gray-800 p-3">
                                <span class="font-mono text-xs text-gray-400">
                                    <%= item.slideId %>
                                </span>
                            </td>
                         
                            
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <% if (data.length === 0) { %>
            <div class="text-center py-8">
                <p class="text-gray-400">No missing fields found in canvas items.</p>
            </div>
        <% } %>
    </div>
</body>
</html>